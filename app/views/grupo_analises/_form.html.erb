<%= form_for(@grupo_analise) do |f| %>
  <% if @grupo_analise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grupo_analise.errors.count, "error") %> prohibited this grupo_analise from being saved:</h2>

      <ul>
      <% @grupo_analise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
